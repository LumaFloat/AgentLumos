{
  "name": "agentlumos",
  "version": "0.4.0",
  "description": "Turn keyboard indicator LEDs into status lights for AI coding agents.",
  "license": "Apache-2.0",
  "bin": {
    "lumos": "./dist/src/cli/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "engines": {
    "node": ">=20"
  },
  "files": [
    "dist",
    "docs",
    "LICENSE",
    "README.md",
    "README.zh-CN.md"
  ],
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0",
    "vitest": "^4.1.9"
  },
  "dependencies": {
    "commander": "^14.0.3",
    "koffi": "^3.0.2"
  }
}
